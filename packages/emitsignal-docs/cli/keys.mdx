---
title: 'keys'
description: 'Create, list, and revoke scoped API keys for CI, servers, and scripts.'
---

import { Cmd, Cmt, Ok, Out, Sp, Term } from '../snippets/terminal.jsx';

```
emitsignal keys <list | create | revoke> [flags]
```

Keys are scoped — a CI key that can only write to `ci/*` can never read your inbox,
subscribe to topics, or touch anything outside its scope.

## Subcommands

| Subcommand    | What it does                                            |
| ------------- | ------------------------------------------------------- |
| `list`        | Show all keys with their IDs (secrets masked)           |
| `create`      | Mint a new key                                          |
| `revoke <id>` | Deactivate a key; run again (or `--force`) to delete it |

The `id` shown by `list` and `create` is what you pass to `revoke`.

`revoke` is a two-step safeguard: the first call **deactivates** the key so it
can no longer authenticate, and a second call on the now-deactivated key
**permanently deletes** it. Pass `--force` to collapse both steps into one.

## Flags

<ParamField body="--name, -n" type="string">
    Human label for a new key. Used in audit logs and `keys list`.
</ParamField>

<ParamField body="--expires, -e" type="duration">
    Auto-revoke after a duration, e.g. `90d`, `1y`. Optional.
</ParamField>

<ParamField body="--force, -f" type="flag">
    On `revoke`, skip the deactivate step and permanently delete the key in one call.
</ParamField>

<ParamField body="--json" type="flag">
    Machine-readable output. Useful for piping to a secrets store.
</ParamField>

## Examples

<Term title="emitsignal — zsh">
    <Cmt># mint a key for CI — the full secret is shown exactly once</Cmt>
    <Cmd wrap>emitsignal keys create -n ci-runner -e 90d</Cmd>
    <Out color="#f5f0ff">
        &nbsp;&nbsp;id&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; hK3n8QpZ2mVx9rLd0a99
    </Out>
    <Out color="#f5f0ff">&nbsp;&nbsp;name&nbsp;&nbsp;&nbsp;&nbsp; ci-runner</Out>
    <Out color="#f5f0ff">&nbsp;&nbsp;expires&nbsp; in 90d</Out>
    <Out>
        &nbsp;&nbsp;secret&nbsp;&nbsp;{' '}
        <span style={{ color: '#fbbf24' }}>es_live_c8f4_9Qk2Lr7vN3pXa1Ze6Bd0Yw</span>
    </Out>
    <Ok>store this now — it will not be shown again</Ok>
    <Sp />
    <Cmt># list shows the id (first column) — that is what revoke takes</Cmt>
    <Cmd>emitsignal keys list</Cmd>
    <Out color="#f5f0ff">
        {'p9k2Lm4vX8nQr1Td0Zs  prod-write    es_live_p9k2_••••   4m ago     active'}
    </Out>
    <Out color="#f5f0ff">
        {'hK3n8QpZ2mVx9rLd0a99  ci-runner     es_live_c8f4_••••   just now   active'}
    </Out>
    <Out color="#b8a9d9">
        {'o12kWn5rT7yUe3Pxa9Q7  old-staging   es_live_o12k_••••   3d ago     revoked'}
    </Out>
    <Sp />
    <Cmt># first revoke deactivates; a second revoke permanently deletes</Cmt>
    <Cmd>emitsignal keys revoke hK3n8QpZ2mVx9rLd0a99</Cmd>
    <Ok wrap>
        deactivated key hK3n8QpZ2mVx9rLd0a99 — run revoke again (or pass --force) to delete it
    </Ok>
    <Cmd>emitsignal keys revoke hK3n8QpZ2mVx9rLd0a99</Cmd>
    <Ok>permanently deleted key hK3n8QpZ2mVx9rLd0a99</Ok>
    <Sp />
    <Cmt># or delete in one step with --force</Cmt>
    <Cmd>emitsignal keys revoke o12kWn5rT7yUe3Pxa9Q7 --force</Cmd>
    <Ok>permanently deleted key o12kWn5rT7yUe3Pxa9Q7</Ok>
</Term>

**Pipe to a secrets manager:**

<Term title="emitsignal — zsh">
    <Cmd wrap>
        emitsignal keys create -n ci-runner --json | jq -r '.key' | gh secret set ES_TOKEN
    </Cmd>
    <Ok>secret ES_TOKEN set for repo</Ok>
</Term>

<Warning>
    The secret is shown **once**, immediately after creation. Store it in a secrets manager before
    closing your terminal. If you lose it, revoke the key and create a new one — there is no way to
    retrieve a secret after the first display.
</Warning>

## Using keys in CI

Set `ES_TOKEN` in your CI environment:

```yaml .github/workflows/deploy.yml
- name: Notify on deploy
  env:
      ES_TOKEN: ${{ secrets.ES_TOKEN }}
  run: |
      emitsignal publish deploy/prod -p4 -m "Deployed ${{ github.sha }}"
```
